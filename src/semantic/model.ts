/**
 * Semantic Model Types (Phase 2B)
 * Inspired by Wren AI semantic layer
 *
 * Defines business concepts (metrics, relationships) so the LLM
 * can reason in business terms instead of raw column names.
 */

export { SemanticModel, SemanticEntity, SemanticMetric, SemanticRelationship } from '../types';
